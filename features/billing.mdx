---
title: Billing
description: Download-first paywall backed by Paddle — webhook fulfillment, entitlement checks, and the customer portal.
---

<Note>
**2026-09-16 (D82 Part 2):** The paywall gate is live. The app checks entitlement at boot, on focus,
on auth change, and on paywall-open events. Signed-out users land in read-only mode; locked users see
a full-screen Paywall. Cached entitlement grants offline read-only with no time limit. The checkout
page at `https://kyrou.org/#/checkout` is on the Paddle sandbox. A real sandbox purchase from the dev
app unlocked the paywall end to end, and the app recorded a trialing subscription. Server-side, push and
conflict resolve return 402 for non-entitled accounts. See D81 and D82 Part 2 in `docs/DECISIONS.md`.
</Note>

## Where checkout runs

- **Website:** `https://kyrou.org`, on the Cloudflare Pages project `kyrou-landing`. `www.kyrou.org`
  redirects there.
- **Environments:** Pages Production talks to the production Worker, and PR preview deployments
  talk to the dev Worker. A checkout ticket only works on the Worker that created it.
- **Desktop app:** opens `https://kyrou.org` by default. A dev build pointed at the dev Worker must
  set `VITE_CHECKOUT_URL` to a preview deployment.
- **Support:** `support@kyrou.org`.

## What it does

Kyrou has no freemium tier. The flow is: download the app → onboarding → paywall → pay on the
website's own `/checkout` page (a Paddle.js overlay) → the app rechecks entitlement and unlocks.

Payment never happens inside the desktop app itself — only a link out to the website checkout and
a link out to Paddle's hosted customer portal for managing an existing subscription.

## Files tied to it

- `workers/kyrou-api/src/routes/paddle-webhook.ts` — `POST /v1/paddle/webhook`
- `workers/kyrou-api/src/routes/billing.ts` — `GET /v1/billing/entitlement`, `POST /v1/billing/portal`
- `workers/kyrou-api/src/billing.ts` — `grantsAccess(status)`
- `database/migrations/0010_billing.sql` — `kyrou.billing_customers`, `kyrou.billing_subscriptions`
- `src/kyrou/billing.ts` — `checkoutUrl()`, `needsPaywall()`
- `src/kyrou/sync/api.ts` — `getEntitlement`, `getBillingPortalUrl`
- `src/app/Paywall.tsx` — the paywall screen (not wired into boot)
- `src/app/SettingsModal.tsx` — "Manage billing" row under Sync & account

## How it works

1. **Webhook.** Paddle posts subscription/customer events to `POST /v1/paddle/webhook`. The
   signature is verified with a hand-rolled HMAC-SHA256 check against every `h1` value in the
   `Paddle-Signature` header (covers secret rotation) — no `@paddle/paddle-node-sdk`, since a plain
   `crypto.subtle` check is smaller and runs natively on Workers. A bad signature always returns
   401, never a 2xx. Events upsert `billing_customers`/`billing_subscriptions`, guarded so a
   stale or replayed event (`occurred_at` older than what's stored) can never overwrite newer state.

2. **Entitlement.** `GET /v1/billing/entitlement` joins subscriptions to customers on the **signed-in
   user's JWT email claim**, not the `user_id` the webhook wrote from checkout's `customData` —
   that field is set client-side during checkout and isn't trustworthy for authorization on its
   own. `grantsAccess(status)` treats `active`, `trialing`, and `past_due` as entitled; `paused` and
   `canceled` are not. A subscription with a scheduled cancellation still grants access until it
   actually lapses.

3. **Portal.** `POST /v1/billing/portal` resolves the Paddle customer the same way (JWT email) and
   returns a hosted customer-portal URL, which Settings opens externally.

4. **Client gate (D82 Part 2).** The app checks entitlement at boot, on focus, on auth change, and on paywall-open events via `PaywallGate.tsx` (mounted in `src/main.tsx`). `checkAccess(apiUrl, token, sub) → ok | past_due | locked` caches the server's last successful answer per user in `localStorage['kyrou-entitlement:<sub>']`; errors, 401/403/5xx, and 10s timeouts never write the cache and fall back to it. No cache means locked.
   - **Locked** → full-screen Paywall showing counts, $9/mo or $90/yr pricing, 7-day free trial (card required), and support contact.
   - **Past_due** → a BillingBanner saying "Payment failed — update payment" (links to the portal).
   - **Signed out** → read-only UI plus a paywall offering "Sign in with Google" or "Continue read-only" (no free tier). "Continue read-only" dismisses until `Settings → Subscribe` or auth changes.
   - **Read-only mode** (`KyrouStore.readOnly`): `save()` blocks with "Read-only — subscribe to edit" and rolls back unsaved edits to the last committed snapshot. Blocks `importBackup()`, undo/redo, and conflict resolve; allows export. The HUD window sets its own read-only from the cache.
   - **Offline access:** A cached entitled answer grants read-only (not full access) with no time limit, so paying users can still review tasks when offline. Tampering with localStorage is an accepted client-side risk because the Worker enforces it.
   - `needsPaywall()` **fails open** on any error (including offline) — a network hiccup can never lock out a user who has already paid.

## Known gaps

- Only the dev database and dev Paddle sandbox are wired up. Production secrets and the
  `0010_billing.sql` migration on the production branch (ep-icy-dew) are still pending — the migration
  must be applied before the Worker deploys, otherwise every push returns 500.
- Onboarding flag hookup (let onboarded users skip the paywall on subsequent launches) is deferred.
- A user who pays with a different email than the one they sign into Kyrou with is never entitled,
  because entitlement is bound to the JWT email. There is no account-linking flow yet.
- Server-side gate: `POST /v1/sync/push` and conflict resolve return `402 {error:'subscription_required'}`
  for non-entitled accounts; pull stays open. The client parks sync on 402, keeps ops queued, still pulls,
  and opens the paywall once.
